{
  "name": "@suss/acceptance",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Runs the built suss binary the way a person runs it, over the repo's fixture projects, and asserts on what comes back. Internal; never published.",
  "license": "Apache-2.0",
  "author": "Nimbus Cloud AI LLC",
  "scripts": {
    "test": "vitest run",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@suss/behavioral-ir": "*",
    "@suss/cli": "*"
  },
  "devDependencies": {
    "typescript": "*",
    "vitest": "*"
  }
}
