node_modules/
dist/
.turbo/
*.tsbuildinfo
.env
**/coverage/*
!**/coverage/coverage-summary.json
!packages/checker/src/coverage/**

# VitePress
docs/.vitepress/dist/
docs/.vitepress/cache/

# Dogfood output (regenerated on demand via `node scripts/dogfood.mjs`)
scripts/dogfood-report.json

# Self-check summaries (regenerated on demand via `npm run check:self`)
.self-check/

# Per-package extraction cache (local to each working tree)
**/.suss/

# External dogfood targets (cloned to repo root for ad-hoc end-to-end runs)
/saleor-dashboard/
/saleor-storefront/
/twenty/

# Claude Code
PLAN.md
CLAUDE.md
.claude/

# Copied from the root LICENSE by scripts/preparePublish.mjs so npm ships one per package.
packages/**/LICENSE
