// The CLI checks a `-f nestjs-microservices=config.json` file against
// `optionsSchema` before it calls the factory.
export {
  declares,
  default,
  optionsSchema,
} from "@suss/framework-nestjs-microservices";
