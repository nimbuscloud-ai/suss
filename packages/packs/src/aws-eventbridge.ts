// The CLI checks a `-f aws-eventbridge=config.json` file against
// `optionsSchema` before it calls the factory.
export {
  declares,
  default,
  optionsSchema,
} from "@suss/framework-aws-eventbridge";
