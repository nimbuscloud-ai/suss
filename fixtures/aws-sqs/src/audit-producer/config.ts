// The queue URL as a named constant, one import away from the send.
// The recognizer resolves this through the store; the summary names
// the literal, the same as writing it at the call.
export const AUDIT_QUEUE_URL =
  "https://sqs.us-east-1.amazonaws.com/123456789012/AuditQueue";
